# Validation

This repository uses GitHub Actions (`.github/workflows/ci.yml`) to install dependencies, type-check the Next.js application, run deterministic unit tests, build the production bundle, and run the demo-mode end-to-end simulation.

Local deterministic scoring/schema tests are also runnable with `npm test` on Node.js 22+.
